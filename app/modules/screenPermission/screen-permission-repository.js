const Repository = container.repository;

/**
 * ScreenPermissionRepository repository
 */
class ScreenPermissionRepository extends Repository {
  /**
   * Init new object
   *
   * @param   {object}  model
   *
   * @return  {void}
   */
  constructor(model) {
    super(model);
  }
}

module.exports = ScreenPermissionRepository;
